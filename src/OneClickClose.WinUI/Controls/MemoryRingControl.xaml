<UserControl
    x:Class="OneClickClose.WinUI.Controls.MemoryRingControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Width="200" Height="200" HorizontalAlignment="Center" VerticalAlignment="Center">
        <!-- Background track (gray circle) -->
        <Ellipse Stroke="{StaticResource StrokeBrush}" StrokeThickness="8"
                 Width="180" Height="180"
                 HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <!-- Value arc (drawn via code-behind Path) -->
        <Path x:Name="ValueArc" Fill="Transparent" StrokeThickness="8"
              HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <!-- Center text -->
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="ValueText" Text="0%"
                       FontSize="28" FontWeight="Bold"
                       Foreground="{StaticResource TitleTextBrush}"
                       HorizontalAlignment="Center"/>
            <TextBlock x:Name="UsedTextBlock" Text="0 / 0 GB"
                       FontSize="13"
                       Foreground="{StaticResource BodyTextBrush}"
                       HorizontalAlignment="Center" Margin="0,2,0,0"/>
            <TextBlock Text="Memory"
                       FontSize="12"
                       Foreground="{StaticResource SubtleTextBrush}"
                       HorizontalAlignment="Center" Margin="0,2,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
